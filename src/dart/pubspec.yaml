name: ores_dnd
version: 0.1.0
description: Pure Dart codec and ports for the ores.dnd/v1 cross-runtime drag-and-drop protocol.
repository: https://github.com/ORESoftware/ores-dnd
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  rxdart: ^0.28.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.0
