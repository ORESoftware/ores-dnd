name: ores_dnd
version: 0.1.0
description: Cross-runtime drag-and-drop protocol and Flutter adapters for ORESoftware apps.
repository: https://github.com/ORESoftware/ores-dnd
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
