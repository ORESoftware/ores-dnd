schema = 2

[tasks.conformance]
description = "Run Zed-resolved TypeSpec/JSON Schema parity plus canonical behavioral conformance"
run = ["sh conformance/check-wrapper.sh"]

[tasks.test]
description = "Run the package-level release test contract"
run = ["sh conformance/check-wrapper.sh"]

[tasks.precommit]
description = "Run staged boundary admission"
run = ["sh .githooks/pre-commit"]

[tasks.prepush]
description = "Exercise the complete Zed lifecycle before push"
run = ["sh .githooks/pre-push"]
