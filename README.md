# ores-dnd

Cross-runtime drag-and-drop contracts and adapters for ORESoftware applications.

Implementation work lands through pull requests against `main`. The public contract is maintained as independent TypeSpec and JSON Schema Draft 2020-12 authorities, with parity enforced by `@oresoftware/typespec-json-schema-validator`.
